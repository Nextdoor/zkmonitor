# General App Requirements
statsd
tornado>=3.1.1
PyYAML

# Note: This is only installable directly from Github, but because
# this file is parsed by setup.py, we cannot use the pip+github
# syntax. Instead, the Makefile handles the installation of this
# dependency.
tornadomail
tornado_rest_client

# Nextdoor Service Registry
nd_service_registry>=1.0.0
